# Some Algorithms Practice in Javascript

**Have used [JEST](https://github.com/facebook/jest) for testing**

**To run a test case:**

```
1. `npm i -g jest` // install jest globally
2. `cd /exercises`
2. `jest dir_name/test.js --watch`// to test in watch mode
```
